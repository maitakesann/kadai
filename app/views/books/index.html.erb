<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="csrf-param"content="authenticity_token">
    <script src="https://kit.fontawesome.com/f1f4eb41b3.js" crossorigin="anonymous"></script>
    <link rel="stylesheet"   href="/assets/stylesheets/application.css">
    <title>WebCampBackwndNew</title>

  </head>
  <body>
  	<p id="notice"></p>
  	<h1>Books</h1>

  	<table>
  	  <thead>
  	    <tr>
  	      <th>Title</th>
  	      <th>Body</th>
  	      <th colspan="3"></th>
  	    </tr>
  	  </thead>

  	  <tbody>
      <% @books.each do |book| %>
  	    <tr>
  	      <td><%= book.title %></td>
  	      <td><%= book.body %></td>
  	      <td>
  	      <%= link_to "Show", book_path(book.id) %>
  	      </td>
  	      <td><%= link_to "Edit", edit_book_path(book.id) %></td>
  	      <td><%= link_to "Destroy", book_path(book), method: :delete %></td>

  	    </tr>
      <%end%>
  	  </tbody>

  	</table>
  	
  	<h1>New Book</h1>
  	 <% if @book.errors.any? %>
  	 <div crass="error_box">
       <div class="error_box_content"><h3><%= @book.errors.count %>errors prohibited this book form being saved:</h3></div>
       <div class="error_box_content_li">
         <ul>
          <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
           <%end%>
         </ul>
        </div>
     <%end%>
     </div>
    <%= form_with model:@book,local:true do |f| %>

      <div class="field">
       <h4>Title</h4>
       <%= f.text_field :title %>
       </div>
      <div class="field">
      <h4>Body</h4>
      <%= f.text_area :body %>
      </div>
      <div class="actions">
        <%= f.submit  'Create Book', class: "btn" %>
      </div>
    <%end%>



  </body>


</html>
